import Logo from '../Logo/Logo';
import ScoreBoard from '../ScoreBoard/ScoreBoard';

const Header = () => {
  return (
    <header>
      <Logo />
      <ScoreBoard />
    </header>
  );
};

export default Header;
